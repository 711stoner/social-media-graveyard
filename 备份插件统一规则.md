# 社媒坟场｜备份插件统一规则

适用于：X、Threads、微博、豆瓣、知乎、小红书，以及以后新增的社交媒体备份插件。

## 1. 基本原则
- 插件用于用户备份自己的账号与内容，默认 Local-first。
- 完整帖子、图片、视频、私信、完整备份包等默认只保存在用户本地。
- “社媒墓碑”是独立的自愿公开功能，默认关闭。
- 未经用户明确确认，不得因为备份行为自动创建墓碑或上传墓碑资料。

## 2. 墓碑自动采集字段
插件在正常备份流程中尽可能自动取得以下信息，并先保存在本地：
- platform：平台名称。
- username：账号用户名 / @username。
- displayName：显示名称 / 昵称。
- avatar：当前主页头像；本地保存头像副本，不能只依赖平台远程 URL。
- profileUrl：账号主页地址。
- accountStartDate：账号起始日期。
- accountStartDateType：日期来源，必须标记为 registered / earliest_post / unknown。
- lastBackupDate：最后一次备份日期。
- archiveCount：已备份或扫描到的公开内容数量。

## 3. 日期规则
- 能可靠读取官方注册时间时，显示“注册于 YYYY.MM”。
- 无法读取注册时间时，以最早可追溯内容日期作为 accountStartDate，并显示“最早记录于 YYYY.MM”。
- 不得把最早内容日期伪装成注册日期。
- 无法可靠获得任何起始日期时使用 unknown，不猜测、不补造。

## 4. 用户必须主动决定的字段
备份完成后，可向用户显示“是否给这个账号留一块墓碑？🪦”。
用户主动选择生成后，再要求：
- graveStatus：准备注销 / 已注销 / 停止更新 / 仅作存档。
- showAvatar：是否公开头像。
- epitaph：墓志铭，可选，允许留空。
- consent：最终公开确认。

插件不得自行判断“准备注销”“已注销”等主观状态。

### 导出完成后的引导
- JSON / HTML / 离线 HTML / PDF 等真正导出成功后，插件可以显示一次“导出完成”提示，并询问“是否给这个账号留一块墓碑？”。
- 默认选择必须是不创建；关闭提示或选择“先不用”不能产生任何网络提交。
- 用户主动选择后，可把 platform、username、displayName、archiveCount、lastBackupDate 等非敏感字段预填到社媒坟场的本地预览页。
- 在公开提交 API 尚未完成前，预填字段通过 URL fragment（#）传递，不能放在查询参数中，避免随网页请求发送到服务器。
- 进入墓碑页仍然只是预览，不等于公开，也不等于上传完整备份。

## 5. 墓碑预览与二次确认
提交前必须先生成完整预览，至少显示：
头像（若用户选择公开）、昵称、@用户名、平台、账号起始日期及其类型、墓碑状态、墓志铭、内容数量、最后备份日期。
用户点击“确认公开这块墓碑”后，才允许向社媒坟场服务器提交。

## 6. 允许上传到墓碑服务器的数据
仅在用户最终确认后上传：
platform、username、displayName、avatar 副本（showAvatar=true 时）、profileUrl、accountStartDate、accountStartDateType、lastBackupDate、archiveCount、graveStatus、epitaph、consent、墓碑生成时间。

严禁随墓碑上传：
- 帖子正文全集
- 原始图片/视频归档
- 私信
- Cookie / Token / 登录凭证
- 完整备份文件
- 与墓碑展示无关的浏览数据

## 7. 头像规则
- 墓碑支持头像，并作为核心视觉元素。
- 用户确认公开头像后，应把头像复制到社媒坟场自己的存储，不长期热链平台头像 URL，避免账号注销后头像失效。
- showAvatar=false 时使用平台图标或默认墓碑占位图。
- 墓碑预览必须让用户明确看到头像是否会公开。

## 8. 墓碑统一数据结构
platform
username
displayName
avatar
profileUrl
accountStartDate
accountStartDateType
lastBackupDate
archiveCount
graveStatus
showAvatar
epitaph
consent
createdAt
graveId

## 9. 墓碑编号
服务器成功创建墓碑后生成唯一 graveId，例如：墓碑 № 000023。
同一账号重复提交时应优先识别已有墓碑，避免无意产生大量重复墓碑；后续提供更新或撤下机制。

## 10. 所有新备份插件的开发要求
从现在开始，所有平台插件在设计数据模型和 UI 时都预留“社媒墓碑”模块，不再作为后期补丁添加。
平台适配层负责读取平台特有字段；墓碑模块使用统一数据结构、统一授权流程和统一 API。
